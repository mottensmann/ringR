
# `ringR`

------------------------------------------------------------------------

## Installation

Install the development version of `ringR` with:

``` r
pak::pak("mottensmann/ringR")
```

## Run shiny app

``` r
library(ringR)
ring_app()
```
